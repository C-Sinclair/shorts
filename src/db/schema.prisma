generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgres"
  url      = env("DATABASE_URL")
}

model Short {
  id          Int      @id @default(autoincrement())
  title       String
  description String
  url         String
  tags        String[]
}
